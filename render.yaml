services:
  - type: web
    name: meu-app-financas-web
    env: static
    buildCommand: echo "no build needed"
    staticPublishPath: build/web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html